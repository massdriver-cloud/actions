name: 'Definition Publish'
description: 'Publish a Massdriver Artifact Definition'
author: 'Massdriver, Inc.'
inputs:
  file:
    description: |
      Path to the file containing the artifact definition to publish.
    required: true
runs:
  using: node20
  main: '../dist/preview_decommission/index.js'
